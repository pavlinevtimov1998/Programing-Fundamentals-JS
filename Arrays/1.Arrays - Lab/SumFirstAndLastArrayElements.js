function sumArrays(arr) {
  let first = arr[0];
  let last = arr[arr.length - 1];

  console.log(first + last);
}
sumArrays([20, 30, 40]);
sumArrays([10, 17, 22, 33]);
sumArrays([11, 58, 69]);
